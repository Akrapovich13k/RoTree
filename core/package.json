{
  "name": "@rotree/core",
  "version": "0.1.2",
  "private": true,
  "description": "Shared RoTree core: HTTP server, export reader, context builder, Rojo comparator, patch manager.",
  "license": "MIT",
  "main": "out/index.js",
  "types": "out/index.d.ts",
  "files": ["out"],
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.4.0"
  }
}
