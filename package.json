{
  "name": "rotree-monorepo",
  "private": true,
  "version": "0.1.2",
  "workspaces": ["core", "cli", "extension"],
  "scripts": {
    "build": "npm run build -w @rotree/core && npm run build -w @rotree/cli && npm run build -w rotree",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -w @rotree/core"
  }
}
